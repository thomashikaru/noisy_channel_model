.vscode

# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Generated outputs / large data
log/
data/

run_native.json
planning/single_token_words.txt

# Personal local demo runner (untracked; edit freely without touching the repo)
run_example_native.sh

# SLURM harness: private per-cluster settings (never commit) + batch outputs
slurm/cluster.env
results_nc/

# Posterior-stability benchmark raw output. The report (planning/BENCHMARK_STABILITY_REPORT.md)
# and the harness (planning/bench_*) are committed; these are regenerable run output.
planning/bench_results/
planning/bench_runtime/