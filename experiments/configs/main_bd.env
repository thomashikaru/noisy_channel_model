# main arm, Gibbs indel rejuvenation. Identical to main_off.env except REJUV (user decision:
# "keep everything else constant" -- run.sh enforces it). ~180 s/item vs ~15-30 s for off.
. "$(dirname "${BASH_SOURCE[0]}")/main.env"
REJUV=gibbs+bd
